node_modules
.DS_Store
dist
dist-ssr
*.local
count.txt
.env
.nitro
.tanstack
.wrangler
.output
.vinxi
todos.json
.vercel/output

*storybook.log
storybook-static

# bv (beads viewer) local config and caches
.bv/

# Cover letters (local only, not deployed)
src/content/coverletters/*.md
src/content/coverletters/*.pdf
