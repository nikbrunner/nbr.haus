---
import profile_picture from "../../components/../assets/images/profile_picture.jpg";
---

<div class="profile-picture">
  <img
    class="profile-image"
    src={profile_picture.src}
    alt="Nik Brunner's personal website - a black and white photograph of a modernist house representing his digital home"
  />
  <div class="profile-image-overlay"../../components/div>
</div>

<style>
  .profile-picture {
    position: relative;
  }

  .profile-image {
    aspect-ratio: 1 / 1;
    width: 100%;
    object-fit: cover;
    display: block;
    box-shadow: var(--shadow-hard);
    border: var(--border-size-1) solid var(--fg-accent);
  }

  .profile-image-overlay {
    position: absolute;
    backdrop-filter: desaturate(0.5) brightness(1.2);
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    background-color: var(--fg-accent);
    opacity: 0.1;
    z-index: 1;
  }
</style>
